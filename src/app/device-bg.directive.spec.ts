import { DeviceBgDirective } from './device-bg.directive';

describe('DeviceBgDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceBgDirective();
    expect(directive).toBeTruthy();
  });
});
